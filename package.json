{
  "name": "jsaqa-7-06",
  "version": "1.0.0",
  "description": "cypress",
  "main": "index.js",
  "scripts": {
    "testChromeHeadMobile": "npx cypress run --browser chrome --headed --config-file cypress.env.json",
    "testChromeHeadless": "npx cypress run --browser chrome --headless",
    "testElectronHeadMobile": "npx cypress run --browser electron --headed --config-file cypress.env.json",
    "testElectronHeadless": "npx cypress run --browser electron --headless"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.7.0"
  }
}
